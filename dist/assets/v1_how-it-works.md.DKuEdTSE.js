import{_ as s,o as a,c as i,a6 as t}from"./chunks/framework.Dtv7txdI.js";const k=JSON.parse('{"title":"How Nitr works","description":"","frontmatter":{},"headers":[],"relativePath":"v1/how-it-works.md","filePath":"v1/how-it-works.md","lastUpdated":1787648967000}'),n={name:"v1/how-it-works.md"};function o(r,e,l,d,h,c){return a(),i("div",null,[...e[0]||(e[0]=[t(`<h1 id="how-nitr-works" tabindex="-1">How Nitr works <a class="header-anchor" href="#how-nitr-works" aria-label="Permalink to “How Nitr works”">​</a></h1><p>One page on the execution model. Everything else in these docs makes more sense once this is clear.</p><h2 id="the-shape-of-a-nitr-process" tabindex="-1">The shape of a Nitr process <a class="header-anchor" href="#the-shape-of-a-nitr-process" aria-label="Permalink to “The shape of a Nitr process”">​</a></h2><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>                     ┌──────────────────────────────────────┐</span></span>
<span class="line"><span>   HTTP  ──────────► │  Rust: hyper, router, static files,  │</span></span>
<span class="line"><span>                     │  CORS, compression, limits, health   │</span></span>
<span class="line"><span>                     └───────────────┬──────────────────────┘</span></span>
<span class="line"><span>                                     │  only a matching dynamic route</span></span>
<span class="line"><span>                                     ▼</span></span>
<span class="line"><span>                     ┌──────────────────────────────────────┐</span></span>
<span class="line"><span>                     │  Lua state pool (one per CPU core)   │</span></span>
<span class="line"><span>                     │  ┌────────┐ ┌────────┐ ┌────────┐    │</span></span>
<span class="line"><span>                     │  │ state1 │ │ state2 │ │ state3 │ …  │</span></span>
<span class="line"><span>                     │  └────────┘ └────────┘ └────────┘    │</span></span>
<span class="line"><span>                     └──────────────────────────────────────┘</span></span>
<span class="line"><span>                              each: app.lua loaded once,</span></span>
<span class="line"><span>                              routes + middleware compiled,</span></span>
<span class="line"><span>                              memory cap + execution budget</span></span></code></pre></div><p>A request is checked out against one state, runs there, and the state goes back to the pool. States never share Lua values with each other.</p><h2 id="the-two-scripts" tabindex="-1">The two scripts <a class="header-anchor" href="#the-two-scripts" aria-label="Permalink to “The two scripts”">​</a></h2><table tabindex="0"><thead><tr><th>Script</th><th>Runs</th><th>Purpose</th></tr></thead><tbody><tr><td><code>config.lua</code></td><td><strong>exactly once</strong>, at startup, before any request</td><td>setup; its return value becomes <code>nitr.cfg</code></td></tr><tr><td><code>app.lua</code></td><td><strong>once per Lua state</strong> (and again on every reload)</td><td>builds and returns the application</td></tr></tbody></table><h3 id="config-lua" tabindex="-1"><code>config.lua</code> <a class="header-anchor" href="#config-lua" aria-label="Permalink to “config.lua”">​</a></h3><div class="language-lua"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- Optional. The database connection arrives as the script&#39;s vararg.</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> db </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">db</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">execute</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;PRAGMA optimize&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    app_name </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;my-app&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    started_at </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nitr.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">time</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">iso8601</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(nitr.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">time</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">now</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()),</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>It must return <strong>plain data</strong> — tables, strings, numbers, booleans. The return value is serialized and snapshotted into every state, so a function or a coroutine in there is an error, not a hidden bug. Every handler then reads it as <code>nitr.cfg</code>.</p><h3 id="app-lua" tabindex="-1"><code>app.lua</code> <a class="header-anchor" href="#app-lua" aria-label="Permalink to “app.lua”">​</a></h3><div class="language-lua"><button title="Copy Code" class="copy"></button><span class="lang">lua</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">local</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> app </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> nitr.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">use</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(some_middleware)     </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- global middleware, before any route</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">get</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/x&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, handler)       </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- routes</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">app</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">on_error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(handler)        </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- the app-wide error response</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> app                   </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">-- ← the script MUST return the app</span></span></code></pre></div><p>This script does not handle a request. It <em>describes</em> the application, once. The router, the middleware chain and the error handler are all composed at load time, so per-request work is just: match, call.</p><div class="tip custom-block github-alert"><p class="custom-block-title">Why this matters for performance</p><p></p><p>Middleware is <code>function(next) return function(req) ... end end</code> — a factory. The outer function runs once, at load; only the inner function runs per request. Expensive setup (compiling a validation schema, building a lookup table) belongs in the outer scope of <code>app.lua</code> or in the factory, never inside the request function.</p></div><h2 id="the-pool-of-lua-states" tabindex="-1">The pool of Lua states <a class="header-anchor" href="#the-pool-of-lua-states" aria-label="Permalink to “The pool of Lua states”">​</a></h2><p>Nitr does not run Lua on one interpreter behind a lock. It creates <code>workers</code> independent states (default: the CPU core count), each having loaded <code>app.lua</code> on its own. A request:</p><ol><li><strong>checks out</strong> a free state — waiting at most <code>[limits] pool_wait_ms</code> before the request is shed with <code>503</code> + <code>Retry-After</code>;</li><li><strong>runs</strong> the middleware chain and handler in that state, under its memory limit and execution budget;</li><li><strong>returns</strong> the state to the pool — unless the state was poisoned (it hit the memory limit, or a panic was contained), in which case it is dropped and rebuilt, never reused.</li></ol><p>Consequences worth internalising:</p><ul><li><strong>Concurrency is bounded by <code>workers</code>.</strong> More concurrent handlers than states means queueing, and past <code>pool_wait_ms</code> it means shedding. That is deliberate backpressure, not a bug.</li><li><strong>Module-level Lua state is per-state, not per-process.</strong> A counter in a Lua upvalue counts what <em>that one state</em> did. For anything shared, use <a href="./server/cache"><code>nitr.cache</code></a> (bounded, shared, plain data) or <a href="./server/database"><code>nitr.db</code></a>.</li><li><strong>Blocking is contagious within a state.</strong> Nitr keeps blocking work off the async threads for you — <code>nitr.db</code> queries run on a blocking pool, <code>nitr.fetch</code> is async, and <a href="./server/streaming">streaming responses</a> hold their state for the stream&#39;s lifetime (which is why <code>max_streams</code> defaults to <code>workers - 1</code>).</li></ul><h2 id="what-never-reaches-lua" tabindex="-1">What never reaches Lua <a class="header-anchor" href="#what-never-reaches-lua" aria-label="Permalink to “What never reaches Lua”">​</a></h2><p>A surprising amount of HTTP is answered in Rust before a Lua state is even checked out:</p><ul><li>static files (with ETag / <code>Last-Modified</code> / <code>304</code> / range requests)</li><li><code>404</code> when nothing matched, <code>405</code> (with <code>Allow</code>) when only the method is wrong, <code>204</code> + <code>Allow</code> for a bare <code>OPTIONS</code></li><li>CORS preflights</li><li><code>/healthz</code> and <code>/readyz</code></li><li>every <a href="./server/configuration/file#limits">limit</a>: oversized URI, headers, body, too many connections, rate limit</li><li>response compression, and precompressed <code>.br</code> / <code>.gz</code> sidecars</li></ul><p>This is why Nitr can be small and still correct: the parts of HTTP that are tedious and easy to get wrong are not written in your handler.</p><h2 id="the-request-lifecycle-end-to-end" tabindex="-1">The request lifecycle, end to end <a class="header-anchor" href="#the-request-lifecycle-end-to-end" aria-label="Permalink to “The request lifecycle, end to end”">​</a></h2><ol><li>Connection accepted (subject to <code>max_connections</code>).</li><li>Headers read within <code>header_read_ms</code>, bounded by <code>max_header_bytes</code> and <code>max_uri_bytes</code>.</li><li>Rate limit checked (<code>[rate_limit]</code>), if enabled.</li><li>Health endpoints, CORS preflight, static mounts — answered here if they match.</li><li>Route matched. No match → <code>404</code>/<code>405</code>, still no Lua.</li><li>A Lua state is checked out (<code>pool_wait_ms</code>, else <code>503</code>).</li><li>Global middleware → route middleware → handler, all inside the state&#39;s execution budget (<code>exec_timeout_ms</code>) and memory limit (<code>memory_limit</code>).</li><li>The returned table becomes the response; body reading and multipart streaming happen against Rust-side limits as the handler asks for them.</li><li>Compression, conditional-request handling and cookie headers applied.</li><li>State returned to the pool; the <code>request</code> span closes, emitting the access-log line.</li></ol><h2 id="reloading-and-shutdown" tabindex="-1">Reloading and shutdown <a class="header-anchor" href="#reloading-and-shutdown" aria-label="Permalink to “Reloading and shutdown”">​</a></h2><ul><li><strong><code>SIGHUP</code></strong> (or <code>nitr reload</code>, which finds the process via its <code>pidfile</code>) rebuilds the Lua pool without dropping connections. The listener and keep-alive connections survive.</li><li><strong><code>--dev</code></strong> watches the scripts, routes and templates directories and does the same rebuild on save.</li><li><strong><code>SIGTERM</code>/<code>SIGINT</code></strong> drains: stop accepting → <code>/readyz</code> flips to <code>503 draining</code> → in-flight requests finish within <code>[shutdown] grace</code> (+ <code>stream_grace</code> for live streams) → exit. A drain that runs out of time exits non-zero, because a cut request is not a clean shutdown.</li></ul><h2 id="where-the-safety-comes-from" tabindex="-1">Where the safety comes from <a class="header-anchor" href="#where-the-safety-comes-from" aria-label="Permalink to “Where the safety comes from”">​</a></h2><table tabindex="0"><thead><tr><th>Risk</th><th>What stops it</th></tr></thead><tbody><tr><td><code>while true do end</code></td><td>instruction-count hook enforcing <code>exec_timeout_ms</code></td></tr><tr><td>runaway allocation</td><td>per-state <code>memory_limit</code> (8 MiB); the state is dropped and rebuilt</td></tr><tr><td>filesystem / process access</td><td><code>io</code> and <code>os</code> are not in the stdlib by default; <code>require</code> is confined to the scripts directory; native modules cannot load</td></tr><tr><td>a huge upload eating the heap</td><td>multipart parts stream to disk <strong>in Rust</strong> and never become Lua strings</td></tr><tr><td>SSRF via <code>nitr.fetch</code></td><td>private/loopback ranges refused inside the resolver the connector uses; every redirect hop re-checked</td></tr><tr><td>one bad request killing the process</td><td>per-request panic containment; the state is recycled, the process lives</td></tr></tbody></table><p>The full picture, including what the sandbox explicitly does <em>not</em> defend against, is in <a href="./server/security">Security &amp; the sandbox</a>.</p>`,30)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
